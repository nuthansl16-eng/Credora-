/**
 * PLACEHOLDER TYPE — regenerate before relying on this in production.
 *
 * The real, fully-typed version of this file should be generated from
 * your actual Supabase schema with:
 *
 *   supabase gen types typescript --project-id <your-project-ref> > lib/supabase/types.ts
 *
 * That command requires network access to your Supabase project, which
 * the environment that generated this codebase did not have. Until you
 * regenerate it, `Database` is intentionally loose so the app compiles,
 * and several call sites use `any` casts on join results as a documented
 * stopgap (see docs/COMPLETION_STATUS.md).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
